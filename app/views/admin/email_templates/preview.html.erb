<!DOCTYPE html>
<html>
<head>
  <title>Preview: <%= @email_template.name %></title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    .preview-header { background: #f5f5f5; padding: 10px; margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="preview-header">
    <strong>Тема:</strong> <%= @preview_subject %>
  </div>

  <div class="preview-body">
    <%= raw @preview_body_html %>
  </div>

  <% if @preview_body_text.present? %>
    <hr>
    <h3>Текстовая версия:</h3>
    <pre><%= @preview_body_text %></pre>
  <% end %>
</body>
</html>
