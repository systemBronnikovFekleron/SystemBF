<div style="padding: 2rem; max-width: 900px; margin: 0 auto;">
  <%= link_to '← Назад', admin_wiki_pages_path, style: 'color: var(--primary); margin-bottom: 2rem; display: block;' %>
  <h1 style="font-size: 1.875rem; font-weight: 700; margin-bottom: 2rem;">Редактировать: <%= @wiki_page.title %></h1>
  <div class="card"><%= render 'form', wiki_page: @wiki_page, parent_pages: @parent_pages %></div>
</div>
