<%
  badge_config = case status
  when 'pending'
    { text: 'Ожидает одобрения', icon: '⏳', color: 'var(--color-warning)' }
  when 'approved'
    { text: 'Одобрена', icon: '✓', color: '#3B82F6' }
  when 'rejected'
    { text: 'Отклонена', icon: '✗', color: 'var(--color-error)' }
  when 'paid'
    { text: 'Оплачена', icon: '💰', color: 'var(--color-success)' }
  when 'completed'
    { text: 'Завершена', icon: '✓', color: 'var(--color-success)' }
  when 'cancelled'
    { text: 'Отменена', icon: '⊘', color: 'var(--gray-500)' }
  else
    { text: status, icon: '•', color: 'var(--gray-500)' }
  end
%>

<span class="badge" style="background: <%= badge_config[:color] %>15; color: <%= badge_config[:color] %>; border: 1px solid <%= badge_config[:color] %>40;">
  <%= badge_config[:icon] %> <%= badge_config[:text] %>
</span>
