# Диагностика: Admin страницы не работают

## ✅ Что проверено и работает:

1. **Routes существуют:**
   ```
   GET /admin/categories → admin/categories#index ✅
   GET /admin/articles → admin/articles#index ✅
   GET /admin/events → admin/events#index ✅
   GET /admin/wiki_pages → admin/wiki_pages#index ✅
   ```

2. **Контроллеры загружаются:**
   ```
   Admin::CategoriesController ✅
   Admin::ArticlesController ✅
   Admin::EventsController ✅
   Admin::WikiPagesController ✅
   ```

3. **Данные загружаются:**
   ```
   Categories: OK ✅
   Articles: OK ✅
   Events: OK ✅
   ```

4. **Views файлы существуют:**
   ```
   app/views/admin/categories/ (5 файлов) ✅
   app/views/admin/articles/ (5 файлов) ✅
   app/views/admin/events/ (6 файлов) ✅
   app/views/admin/wiki_pages/ (6 файлов) ✅
   ```

5. **Ссылки в sidebar существуют:**
   ```erb
   <%= link_to admin_categories_path ... %> ✅
   <%= link_to admin_articles_path ... %> ✅
   <%= link_to admin_events_path ... %> ✅
   <%= link_to admin_wiki_pages_path ... %> ✅
   ```

---

## 🔍 Что нужно проверить:

### 1. Запустите сервер и проверьте каждую страницу:

```bash
rails server
```

**Откройте в браузере:**
- http://localhost:3000/admin/categories
- http://localhost:3000/admin/articles
- http://localhost:3000/admin/events
- http://localhost:3000/admin/wiki_pages

### 2. Возможные проблемы:

#### A. Ошибка 404 (страница не найдена)
**Причина:** Возможно, нужно перезапустить сервер после добавления routes

**Решение:**
```bash
# Остановить сервер (Ctrl+C)
# Запустить снова
rails server
```

#### B. Ошибка доступа (Unauthorized)
**Причина:** Пользователь не имеет admin_role?

**Проверка:**
```bash
rails console
user = User.find_by(email: 'your@email.com')
user.admin_role?  # должно вернуть true
```

**Если false, установите admin роль:**
```bash
user = User.find_by(email: 'your@email.com')
user.update(classification: :admin)
```

#### C. Ошибка в view (500 Internal Server Error)
**Проверка:** Смотрите Rails server logs в терминале

**Возможные причины:**
- Отсутствие данных (пустые таблицы)
- Ошибка в ERB template
- Неопределенные переменные

#### D. Страница не загружается (белый экран)
**Причина:** Возможно, JavaScript ошибка

**Решение:**
1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Проверьте ошибки JavaScript

---

## 🛠️ Быстрые фиксы:

### Если вижу ошибку "undefined method" в view:

**Проверьте, что контроллер устанавливает все нужные переменные:**

```ruby
# app/controllers/admin/categories_controller.rb
def index
  @categories = Category.ordered.includes(:products)
  @categories_count = @categories.count
  @products_count = Product.count
end
```

### Если вижу ошибку "Can't find Category with 'id'=...":

**Возможно, нет данных в БД. Создайте тестовую категорию:**

```bash
rails console
Category.create!(name: "Тестовая категория", position: 1)
```

### Если sidebar не показывает новые разделы:

**Проверьте, что вы вошли как admin:**

```bash
rails console
User.last.update(classification: :admin)
```

---

## 📋 Пошаговая проверка:

### Шаг 1: Проверьте, что сервер запущен
```bash
rails server
```

### Шаг 2: Войдите как admin
```
Email: admin@bronnikov.com
Password: password123
```

### Шаг 3: Перейдите в админку
```
http://localhost:3000/admin
```

### Шаг 4: Кликните на каждую ссылку в sidebar:
- ✓ Категории
- ✓ Статьи
- ✓ События
- ✓ База знаний

### Шаг 5: Если видите ошибку:

**Скопируйте:**
1. URL страницы
2. Текст ошибки
3. Rails server logs (из терминала)
4. Browser console errors (F12 → Console)

И предоставьте эту информацию для диагностики.

---

## 🔧 Если ничего не помогает:

### Проверьте Rails logs:

```bash
tail -f log/development.log
```

Затем откройте проблемную страницу и посмотрите, что пишется в логах.

### Запустите Rails консоль для ручной проверки:

```bash
rails console
```

```ruby
# Проверка моделей
Category.count
Article.count
Event.count
WikiPage.count

# Проверка контроллера
controller = Admin::CategoriesController.new
controller.class.name
# => "Admin::CategoriesController"

# Проверка routes
Rails.application.routes.url_helpers.admin_categories_path
# => "/admin/categories"
```

---

## ✅ Контрольный чеклист:

- [ ] Сервер запущен (`rails server`)
- [ ] Вошел как admin пользователь
- [ ] Открыл /admin
- [ ] Вижу sidebar с новыми разделами (Категории, Статьи, События, База знаний)
- [ ] Кликнул на "Категории" → открылась страница /admin/categories
- [ ] Кликнул на "Статьи" → открылась страница /admin/articles
- [ ] Кликнул на "События" → открылась страница /admin/events
- [ ] Кликнул на "База знаний" → открылась страница /admin/wiki_pages

Если что-то не работает, предоставьте **конкретную ошибку** или **скриншот**.

---

**Created:** 2026-02-05
**Status:** Awaiting specific error details
